const nav = (state) => {
  if(state == 'close'){
    document.getElementById('nav').style.left = '-250px';
  }else if(state = 'close'){
    document.getElementById('nav').style.left = '0px';
  }
}
